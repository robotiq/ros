<?xml version="1.0"?>
<package format="3">
  <name>robotiq_tsf</name>
  <version>1.1.0</version>
  <!-- Humble EOL: simplify the distro list below. -->
  <description>ROS 2 tactile sensing package for the Robotiq TSF-85 (Humble, Jazzy, Lyrical).</description>

  <maintainer email="jean-philippe.roberge@etsmtl.ca">Jean-Philippe Roberge</maintainer>
  <maintainer email="jenkwia@example.com">Jen Kwia</maintainer>

  <author email="jean-philippe.roberge@etsmtl.ca">Jean-Philippe Roberge</author>
  <author email="jenkwia@example.com">Jen Kwia</author>

  <license>BSD-3-Clause</license>

  <url type="website">https://github.com/ets-laboratory/tactilesensors4</url>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <build_depend>rosidl_default_generators</build_depend>
  <build_depend>std_msgs</build_depend>

  <build_export_depend>rosidl_default_generators</build_export_depend>
  <build_export_depend>std_msgs</build_export_depend>

  <exec_depend>rclcpp</exec_depend>
  <exec_depend>std_msgs</exec_depend>
  <exec_depend>sensor_msgs</exec_depend>
  <exec_depend>ament_index_cpp</exec_depend>
  <exec_depend>rosidl_default_runtime</exec_depend>

  <!-- Quaternion algebra in the AHRS/algorithms library (header-only). -->
  <depend>eigen</depend>

  <!-- SDK-backed poller (poll_data_sdk_node) links the tactile_sensors SDK,
       which uses libserialport. rosdep has no runtime-only key, so the -dev
       package is the exec dependency too. -->
  <depend>libserialport-dev</depend>

  <!-- tactile_viz_node (Python RViz visualization) + its launch/rviz. -->
  <exec_depend>rclpy</exec_depend>
  <exec_depend>geometry_msgs</exec_depend>
  <exec_depend>visualization_msgs</exec_depend>
  <exec_depend>tf2_ros</exec_depend>
  <exec_depend>python3-numpy</exec_depend>
  <exec_depend>launch</exec_depend>
  <exec_depend>launch_ros</exec_depend>
  <exec_depend>rviz2</exec_depend>
  <!-- gripper_tactile_viz.launch.py includes the gripper bringup. -->
  <exec_depend>robotiq_description</exec_depend>

  <test_depend>ament_cmake_gtest</test_depend>
  <test_depend>ament_cmake_pytest</test_depend>
  <test_depend>python3-pytest</test_depend>
  <test_depend>ament_lint_auto</test_depend>
  <test_depend>ament_lint_common</test_depend>

  <member_of_group>rosidl_interface_packages</member_of_group>


  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
